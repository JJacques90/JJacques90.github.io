const setup = () => {
    // window.alert('Dit is een melding!');
    // window.confirm('Weet u het zeker?');
    // console.log(window.prompt('Geef een waarde in'));
}
window.addEventListener("load", setup);